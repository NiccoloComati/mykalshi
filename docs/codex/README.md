# Codex Context

This directory stores persistent engineering context for the repo.

- `current-context.md` is the active handoff for the next Codex run.
- `changes/` contains commit-aligned notes for significant slices of work.

When the repo changes materially, update this directory in the same slice as the code change so the history and the implementation stay aligned.
